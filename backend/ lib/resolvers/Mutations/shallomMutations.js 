
// Mutation resolvers for model: shallom

/**
 * 
 * @param {*} _ => parent
 * @param {*} args 
 * @param {*} => request context (ctx) 
 * @param {*} info 
 */
const createShallom = async (_, args, ctx, info) => {
    try{
        // Add resolver logic here
        return null;
    } catch(err){
        throw err;
    }
}


/**
 * 
 * @param {*} _ => parent
 * @param {*} args 
 * @param {*} => request context (ctx) 
 * @param {*} info 
 */
const updateShallom = async (_, args, ctx, info) => {
    try {
        // Add resolver logic here
        return null;
    } catch (err) {
        throw err;
    }
}


/**
 * 
 * @param {*} _ => parent
 * @param {*} args 
 * @param {*} => request context (ctx) 
 * @param {*} info  
 */
const deleteShallom = async (_, args, ctx, info) => {
    try {
        // Add resolver logic here
        return null;
    } catch(err){
        throw err;
    }
}

module.exports = {
    createShallom,
    updateShallom,
    deleteShallom
}